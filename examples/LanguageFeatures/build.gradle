plugins {
    id "checkstyle"
    id "java"
    id "org.jetbrains.kotlin.jvm" version "$kotlinVersion"
}

group "$appGroup"
version "$appVersion"

description """Kotlin/JVM example project"""

apply from: "../common.gradle"

run.doFirst {
    main kotlinMainClassName
    args ""
}
